package de.thekid.dialog.scriptlet;

import de.thekid.dialog.IEntry;

/**
 * Node handler
 *
 * @see      xp://de.thekid.dialog.scriptlet.StaticState
 * @see      xp://de.thekid.dialog.scriptlet.TopicState
 */
public interface NodeHandler {

  /**
   * Produces the node
   *
   */
  public xml.Node nodeFor(IEntry? $collection, [:self]? $nodeHandlers);
 
}
